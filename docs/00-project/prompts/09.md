# 09 — Testing & Deployment

## Goal
Close out remaining test coverage and prepare for deployment.

## Blueprint Reference
§10 (Cross-Tenant Isolation Testing), §11 step 9.

## Scope
- Full pass over the cross-tenant isolation suite (§10) against every resource, route, and channel built across prompts 01–08 — not just the ones added most recently.
- Order-flow end-to-end tests (online order → preparing → ready → completed → paid).
- Inventory-concurrency tests under realistic parallel load, not just the two-request case from prompt 06.
- Wildcard SSL + dynamic subdomain setup for deployment.

## How to work
Use the `plan-council-code-test` skill. Treat this as an audit task, not new-feature work — the council should be actively looking for gaps left by earlier prompts (e.g. a resource added in prompt 06 that never got an explicit isolation test), not just approving whatever's proposed.

## Documentation (via Tolaria MCP)
- Write a closing daily log to `docs/03-daily-logs/` summarizing what shipped across all 9 prompts and any known gaps or deferred items (cross-reference §12, Explicitly Out of Scope, from the blueprint).
- Confirm every ADR written across prompts 01–08 is present in `docs/00-project/architecture-decisions/` — this is a good point to do a quick documentation audit.

## Update CLAUDE.md
Do a final pass over CLAUDE.md — remove anything stale, confirm the "Data Model Conventions," "Multi-Tenancy Rules," and "Testing Conventions" sections still accurately reflect the shipped code.

## On completion
Full Pest suite passes, cross-tenant isolation suite covers every resource built. This is the last prompt in the sequence — report a final summary of what was built against the original blueprint (docs referenced in the repo's `docs/` root).
