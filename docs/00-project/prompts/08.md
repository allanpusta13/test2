# 08 — Testing & Deployment

## Goal
Close out remaining test coverage and prepare for deployment.

## Blueprint Reference
§8 (Testing — RBAC, replacing the old cross-tenant isolation suite), §9 step 8.

## Scope
- Full pass over RBAC tests against every route built across prompts 01–07 — not just the ones added most recently. Every role-gated route needs a test proving the *wrong* roles are denied, not just that the right one is allowed.
- Order-flow end-to-end tests (online order → preparing → ready → completed → paid), both Pest backend and Pest browser tests.
- Frontend production build check (`npm run build`) confirms cleanly with no missing assets.
- Inventory-concurrency tests under realistic parallel load, not just the two-request case from prompt 05.
- Standard SSL/deployment setup — no wildcard subdomain configuration needed, since this is a single-domain application.

## How to work
Use the `plan-council-code-test` skill. Treat this as an audit task, not new-feature work — the council should be actively looking for gaps left by earlier prompts, not just approving whatever's proposed.

## Documentation
- Write a closing daily log to `docs/03-daily-logs/` summarizing what shipped across all 8 prompts and any known gaps or deferred items (cross-reference blueprint §10, Explicitly Out of Scope — multi-tenancy chief among them).
- Confirm every ADR written across prompts 01–07 is present in `docs/00-project/architecture-decisions/`.

## Update CLAUDE.md
Do a final pass — remove anything stale, confirm every section accurately reflects the shipped code.

## On completion
Full test suite passes (Pest + Vitest), RBAC coverage confirmed for every route, production build succeeds. This is the last prompt in the implementation sequence — report a final summary of what was built against the v4 blueprint.